name: easy-dataset
services:
  easy-dataset:
    image: registry.cn-hangzhou.aliyuncs.com/ripper/easy-dataset:latest
    container_name: easy-dataset
    ports:
      - '1717:1717'
    volumes:
      - ${LOCAL_DB_PATH}:/app/local-db
    restart: unless-stopped
